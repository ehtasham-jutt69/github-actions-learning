name: Linux Practice

on:
  push:

jobs:
  linux-job:

    runs-on: ubuntu-latest

    steps:

      - name: Current Directory
        run: pwd

      - name: Current User
        run: whoami

      - name: Current Date
        run: date

      - name: List Files
        run: ls -la